name: Deploy to AWS Elastic Beanstalk

on:
  push:
    branches:
      - master
    paths:
      - "temp 2/*"

jobs:
  deploy:
    name: Deploy to AWS Elastic Beanstalk
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Say Hello
        run: |
          echo "Hello"
